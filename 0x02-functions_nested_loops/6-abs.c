#include "main.h"

/**
 * _abs - Computes the abdolute value of an interger.
 * @n: The int to check
 * Return: Absolute value of int
 */

int _abs(int n)

{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
